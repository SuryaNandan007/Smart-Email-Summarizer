# GitHub ready commands (run inside the repo folder)

git init
git add .
git commit -m "Initial commit — Smart Email Summarizer Agent"
# Replace <your-remote-url> with your GitHub repository URL:
git remote add origin <your-remote-url>
git branch -M main
git push -u origin main
